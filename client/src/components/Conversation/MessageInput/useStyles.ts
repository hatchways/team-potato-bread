import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  inputMessageWrap: {
    height: '90px',
  },
  inputMessage: {
    width: '100%',
    height: 30,
    borderRadius: 20,
    fontSize: '1.2rem',
  },
}));

export default useStyles;
